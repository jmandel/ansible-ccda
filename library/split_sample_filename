#!/usr/bin/python

import sys, glob, os, sys, json

args_file = sys.argv[1]
args_data = file(args_file).read()

f = args_data.split("=")[1]
base = os.path.basename(f)
stripped = os.path.splitext(base)[0]

ret = {
    'pid': stripped.split("-")[1],
    'file': base 
    }
print json.dumps(ret)
